spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}/${DB_NAME}?reconnect=true
    username: ${DB_USER}
    password: ${DB_PASS}    
    initialization-mode: always    
    data: ${MYSQL_INITIALIZATION_SCRIPT}
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect    